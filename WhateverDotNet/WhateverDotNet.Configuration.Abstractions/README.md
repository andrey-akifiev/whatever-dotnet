# WhateverDotNet.Configuration.Abstractions

Small set of configuration abstractions shared across the WhateverDotNet ecosystem.

## Test environments

The `TestEnvironment` enum represents common execution targets used in tests and tooling.

```csharp
using WhateverDotNet.Configuration;

var env = TestEnvironment.QA;
```

