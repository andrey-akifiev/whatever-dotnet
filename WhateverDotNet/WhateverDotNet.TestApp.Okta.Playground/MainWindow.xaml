<Window x:Class="WhateverDotNet.TestApp.Okta.Playground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:WhateverDotNet.TestApp.Components.Controls;assembly=WhateverDotNet.TestApp.Components"
        mc:Ignorable="d"
        Title="OKTA Authentication Playground"
        Height="600"
        Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="OKTA configuration"
                   FontWeight="SemiBold"
                   FontSize="14"
                   Margin="0 0 0 12"/>

        <StackPanel Grid.Row="1" Margin="0 0 0 8">
            <TextBlock Text="OKTA domain (e.g. https://your-domain.okta.com or https://your-domain.okta.com/oauth2/default)" TextWrapping="Wrap" Margin="0 0 0 2"/>
            <TextBox Text="{Binding OktaDomain, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8 6"
                     MinHeight="28"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 8">
            <TextBlock Text="Client ID" Margin="0 0 0 2"/>
            <TextBox Text="{Binding ClientId, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8 6"
                     MinHeight="28"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0 0 0 8">
            <TextBlock Text="Redirect port (localhost callback, e.g. 8412)" Margin="0 0 0 2"/>
            <TextBox Text="{Binding RedirectPort, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8 6"
                     MinHeight="28"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0 0 0 12">
            <TextBlock Text="Scope (e.g. openid profile)" Margin="0 0 0 2"/>
            <TextBox Text="{Binding Scope, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8 6"
                     MinHeight="28"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0 0 0 8">
            <controls:SignInIconButton Command="{Binding SignInCommand}"
                                      Size="18"/>
            <TextBlock Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       Margin="12 0 0 0"
                       Foreground="{DynamicResource Foreground_Text_LabelBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="6">
            <TextBlock Text="Access token (after sign-in)"
                       FontWeight="SemiBold"
                       Margin="0 0 0 6"/>
            <TextBox x:Name="TokenTextBox"
                     Text="{Binding AccessToken, Mode=OneWay}"
                     AcceptsReturn="True"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Padding="8"
                     MinHeight="120"
                     VerticalContentAlignment="Top"
                     FontFamily="Consolas"
                     FontSize="12"/>
        </StackPanel>
    </Grid>
</Window>
