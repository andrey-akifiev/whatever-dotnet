<UserControl x:Class="WhateverDotNet.TestApp.Components.Controls.Views.ExcelReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:controls="clr-namespace:WhateverDotNet.TestApp.Components.Controls"
             xmlns:converters="clr-namespace:WhateverDotNet.TestApp.Components.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WhateverDotNet.TestApp.Components.Controls.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
                    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:BrowseItemInput
            BrowseCommand="{Binding BrowseFileCommand}"
            CopyToClipboardTemplate="{Binding BrowseFileCopyTemplate}"
            Grid.Column="0"
            Grid.Row="0"
            IsEnabled="{Binding
                Converter={StaticResource InverseBooleanConverter},
                Path=IsLoading,
                Mode=OneWay}"
            Margin="5 5 5 0"
            Path="{Binding FilePath, Mode=TwoWay}"
            Tag="Excel report"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <controls:StringComboBox
                CopyToClipboardTemplate="{Binding SelectedWorksheetCopyTemplate}"
                Grid.Column="0"
                IsEnabled="{Binding
                    Converter={StaticResource InverseBooleanConverter},
                    Path=IsLoading,
                    Mode=OneWay}"
                Items="{Binding Worksheets, Mode=OneWay}"
                Margin="5 0 0 0"
                SelectedValue="{Binding SelectedWorksheet, Mode=TwoWay}"
                Tag="Worksheets"/>
        </Grid>

        <controls:Spinner
            Grid.Row="2"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
