<UserControl x:Class="WhateverDotNet.TestApp.Excel.Components.ReportSpecificationDetails"
             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             
             xmlns:controls="clr-namespace:WhateverDotNet.TestApp.Components.Controls;assembly=WhateverDotNet.TestApp.Components"
             xmlns:local="clr-namespace:WhateverDotNet.TestApp.Excel.Components"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/WhateverDotNet.TestApp.Components;component/Assets/Styles/TextBoxStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <!-- DETAILS COMPONENT ITSELF -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Caption row -->
                <RowDefinition Height="Auto"/> <!-- Main properties -->
                <RowDefinition Height="Auto"/> <!-- Additional properties -->
                <RowDefinition Height="*"/> <!-- Worksheets -->
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock 
                FontFamily="{DynamicResource RegularFontFamily}"
                FontSize="{DynamicResource PageNameFontSize}"
                
                Grid.Row="0"
                
                Margin="0 0 0 10"
                
                Text="{Binding Title}"/>

            <controls:RegularTextBox
                Grid.Column="0"
                Grid.Row="1"
                Margin="5"
                MaxWidth="350"
                
                LabelValue="Name"
                OriginalValue="Original"
                PlaceholderValue="Name"
                ShowClear="True"
                ShowCopy="True"
                ShowRevert="True"
                Text="{Binding Title}"/>

            <controls:RegularTextBox
                Grid.Column="0"
                Grid.Row="2"
                Margin="5"
                MaxWidth="350"
                
                LabelValue="Description"
                OriginalValue="Original"
                PlaceholderValue="Description"
                
                Text="{Binding Description}"/>

            <ScrollViewer
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Grid.Row="3"
                HorizontalScrollBarVisibility="Hidden"
                VerticalScrollBarVisibility="Visible">
                <ItemsControl
                    ItemsSource="{Binding Worksheets}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:ReportSpecificationWorksheetDetails/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            
        </Grid>
    </Grid>
</UserControl>